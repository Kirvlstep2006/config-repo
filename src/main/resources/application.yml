server:
  port: 8888
spring:
  application:
    name: config_server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Kirvlstep2006/config-repo.git
          default-label: main
          skip-ssl-validation: false
          clone-on-start: true
          search-paths: '*'

